[package]
name = "test-core"
version = "0.1.0"
edition = "2024"

[dependencies]

log = { workspace = true }
env_logger = "0.11"

bldc-driver-hal = { path = "../bldc-driver-hal" }
bldc-driver-core = { path = "../bldc-driver-core" }
